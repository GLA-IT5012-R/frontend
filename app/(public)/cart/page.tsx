'use client';
import { Bounded } from "@/components/Bounded";

const Cart = () => {

  return (
    <Bounded className="bg-texture bg-brand-gray">
      <div></div>
    </Bounded>
  );
};

export default Cart;
